# Test Cases

Generated by Forge before code generation to validate the solution.

| # | File | Test Case | Criteria |
|---|------|-----------|----------|
| 1 | `index.html` | Verify multiple football watermark elements exist in HTML | Document contains at least 3 and at most 8 football watermark elements with unique identifiers or data attributes, each referencing the official 2026 World Cup ball image asset |
| 2 | `index.html` | Verify each football watermark has unique CSS animation applied | Each football watermark element has a distinct animation-name, animation-duration (range 8s–20s), and animation-delay value; no two balls share identical timing parameters |
| 3 | `index.html` | Verify CSS keyframe animations are defined with motion effects | CSS contains @keyframes rules with translate, rotate, and opacity properties; at least 3 unique keyframe animations exist with varied motion paths and opacity ranges between 0.06–0.10 |
| 4 | `index.html` | Verify watermark balls have appropriate size and opacity styling | Each football watermark element has width/height between 60–120px and opacity value between 0.06–0.10; z-index is set lower than main content to keep balls in background |
| 5 | `index.html` | Verify staggered positioning prevents synchronized movement | Each football watermark has unique top and left starting positions; animation-delay values are distributed non-uniformly across all balls to create organic scattered appearance |
| 6 | `index.html` | Verify animations are continuous and loop correctly | All @keyframes animations have animation-iteration-count set to infinite; animations loop seamlessly without visual jumps or breaks |
| 7 | `README.md` | Verify README documentation includes watermark feature details | Documentation describes the animated background watermark feature, specifies number of balls added, size range (60–120px), animation duration range (8s–20s), and includes instructions for tweaking animation parameters |
| 8 | `index.html` | Verify watermark elements do not obstruct main content interactivity | Football watermark elements have pointer-events set to none; CSS z-index places watermarks below all interactive foreground elements; layout does not shift when animations run |
