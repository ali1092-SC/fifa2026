# Test Cases

Generated by Forge before code generation to validate the solution.

| # | File | Test Case | Criteria |
|---|------|-----------|----------|
| 1 | `index.html` | Verify Official FIFA 2026 Azemoon Ball Image is Linked | Confirm that the watermark background-image references the official FIFA World Cup 2026 'Azemoon' ball image URL (not a generic or placeholder football image). Verify the image source is correct and accessible. |
| 2 | `index.html` | Verify Watermark Size is Increased | Confirm that the watermark CSS background-size property is increased to a larger value (e.g., 200px, 300px, or percentage > 100%) compared to the previous size. Size should be significantly more visible than before. |
| 3 | `index.html` | Verify Watermark Opacity is Within Acceptable Range | Confirm that the watermark opacity value is between 0.08 and 0.20 (subtle but more visible than before). Extract opacity from CSS and validate it falls within the acceptable range for background watermarks. |
| 4 | `index.html` | Verify Watermark Positioning and Centering | Confirm that the watermark uses CSS properties (background-position: center or background-attachment: fixed) to keep the football centered and properly positioned as a fixed/absolute background element. Verify position values are set correctly. |
| 5 | `index.html` | Verify Watermark Does Not Interfere with Foreground Content | Confirm that the watermark has appropriate z-index, position, and pointer-events settings to ensure it does not block or interfere with interactive foreground elements. Text and buttons must remain fully functional and readable. |
| 6 | `docs/reference files (0.08 and 0.12)` | Verify Opacity Tracking Files Updated | Confirm that opacity reference files are updated to reflect the new watermark opacity values used in the implementation. Files should contain the current opacity percentage/decimal value. |
| 7 | `README.md, docs/FORGE_SESSION.md, docs/FORGE_WIKI.md` | Verify Documentation Updated for Watermark Changes | Confirm that all documentation files have been updated to reflect the larger watermark size, the official Azemoon ball image, and any changes to opacity values. Documentation should describe the visual enhancement. |
| 8 | `index.html` | Verify Watermark Image Renders Correctly on Page Load | Confirm that when the page loads, the watermark displays the Azemoon football image at the increased size with correct opacity. Image should load without errors and display as a background element behind all content. |
