# Test Cases

Generated by Forge before code generation to validate the solution.

| # | File | Test Case | Criteria |
|---|------|-----------|----------|
| 1 | `index.html` | TC-001: fetchWithFallback() successfully retrieves live data from primary API | Verify that fetchWithFallback('games') returns valid JSON array with match objects containing id, home_team, away_team, status, home_score, away_score, and kickoff_time fields. Response time < 3 seconds. No console errors. |
| 2 | `index.html` | TC-002: fetchWithFallback() implements exponential backoff retry on primary API failure | Mock primary API to fail (CORS or 500 error), verify fallback endpoint is called within 2 seconds, returns valid data, and exponential backoff timer (15s → 30s → 60s) is correctly logged in console. sessionStorage cache is populated. |
| 3 | `index.html` | TC-003: sessionStorage caching prevents redundant API calls within 5-minute window | Call fetchWithFallback('games') twice within 5 minutes, verify second call reads from sessionStorage key 'wc_games' and does not trigger network request. Network tab shows only 1 request. Cache expiry triggers refresh after 5 minutes. |
| 4 | `index.html` | TC-004: Live Scores Banner renders 6 live/upcoming matches with correct status badges | Verify Live Scores Banner section contains: (1) Red-pulse LIVE badge for status='live', (2) Countdown timer (mm:ss format) for status='upcoming', (3) '- vs -' placeholder with KO time for status='scheduled', (4) Country flags for both teams, (5) Score display updates every 30 seconds without page reload. |
| 5 | `index.html` | TC-005: Group Standings render all 12 groups with correct qualification badges and left-border colors | Verify all 12 group cards (A–L) are rendered, each displays team table with pts/gd/w-d-l, green left-border for position 1, amber for position 2, grey for 3–4, '✓ QUALIFIED' badge appears when played===3 and position<=2, skeleton loader shows while fetching, no duplicate groups. |
| 6 | `index.html` | TC-006: Live Match Feed Section displays top 6 matches with score, events, possession bars, and auto-refresh | Verify Live Match Feed (#live-feed) contains: (1) Up to 6 match cards, (2) Score in Bebas Neue 48px gold, (3) Event pills with ⚽ 🟨 🟥 icons, (4) Possession/shots mini-bars (width: % value), (5) Left border color-coded (red=live, gold=completed), (6) Auto-refresh every 30 seconds with 400ms opacity pulse on data change, (7) No duplicates, (8) Positioned between Group Standings and Player Spotlight sections. |
| 7 | `index.html` | TC-007: Fixtures & Results Tab System filters matches by date, group, and knockout stage | Verify tabbed interface with KEY DATES, FIXTURES, RESULTS tabs: (1) FIXTURES tab has filter buttons (ALL/TODAY/GROUP A–L/KNOCKOUT), (2) TODAY filter shows only matches with kickoff_time date === today, (3) GROUP filter shows only matches in that group, (4) KNOCKOUT filter shows matches where round contains 'Round of', 'QF', 'SF', 'Final', (5) Results tab displays winner name in gold, loser in grey, (6) No JS errors on tab switching. |
| 8 | `index.html` | TC-008: Golden Boot Tracker aggregates goals from completed matches and renders top 5 scorers | Verify Golden Boot Tracker section: (1) Parses goal events from GET /get/games completed matches, (2) Aggregates goals by player_name and player_nation, (3) Renders top 5 scorers with rank, flag emoji, player name, nation, goal count in Bebas Neue 28px gold, (4) Rank 1 has gold left-border, ranks 2–5 have silver, (5) Located below 6 Player Spotlight cards, (6) Falls back to error card with retry button if data unavailable, (7) Updates every 30 seconds. |
| 9 | `index.html` | TC-009: scheduleRefresh() orchestrator coordinates all sections without memory leaks | Verify scheduleRefresh() function: (1) Calls refreshAll() on first load, (2) Sets polling interval to 30s when any match status='live', else 5min, (3) Clears previous timeouts before setting new ones (no duplicate timers), (4) All five sections (Banner, Groups, Feed, Fixtures/Results, Golden Boot) update simultaneously without console errors, (5) Memory heap does not increase after 10 refresh cycles. |
| 10 | `index.html` | TC-010: renderError() helper displays retry button with exponential backoff countdown | Verify renderError(containerId, retryFn, countdown) helper: (1) Renders error message in target container, (2) Displays 'Retry in Xs' countdown, (3) Countdown decrements every 1 second, (4) Retry button becomes clickable when countdown reaches 0, (5) Clicking retry calls retryFn and resets countdown, (6) Exponential backoff sequence: 15s → 30s → 60s on successive retries, (7) No duplicate countdowns running. |
| 11 | `index.html` | TC-011: countryCodeToFlag() utility converts 3-letter ISO code to correct emoji flag | Verify countryCodeToFlag() converts: USA→🇺🇸, CAN→🇨🇦, MEX→🇲🇽, GER→🇩🇪, FRA→🇫🇷, BRA→🇧🇷, ARG→🇦🇷, ENG→🇬🇧, invalid codes return ❓, function is case-insensitive. |
| 12 | `index.html` | TC-012: All sections degrade gracefully when API is unreachable for 60+ seconds | Mock API downtime >60s, verify: (1) All 5 sections display error card with 'Data temporarily unavailable' message, (2) Retry buttons appear on each section, (3) Countdown timer shows 60s, (4) No raw JS errors in console, (5) Page remains interactive and responsive, (6) Cached data (if available) displays with 'Last updated X minutes ago' badge. |
